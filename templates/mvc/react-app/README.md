# React-App Template (based on Professional Go Architecture)

This template provides the hybrid Go + Laravel-friendly project layout described in professional-go-architecture.md. Use the gophant CLI to scaffold a new project from this template:

    gophant create react-app <project-name>

Replace placeholders in the generated project (module name, env vars) as needed.
